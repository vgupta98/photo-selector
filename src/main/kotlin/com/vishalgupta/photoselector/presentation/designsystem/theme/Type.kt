package com.vishalgupta.photoselector.presentation.designsystem.theme

import androidx.compose.material3.Typography

/**
 * Centralized type scale. Currently the Material3 baseline — kept as a single
 * named value so future type tweaks land here rather than in screen code.
 */
val AppTypography = Typography()
